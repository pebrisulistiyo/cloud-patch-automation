# Copy to backend.hcl and fill in, then: terraform init -backend-config=backend.hcl
# backend.hcl is gitignored. GCS buckets are globally unique.
bucket = "your-state-bucket-gcp"
prefix = "cloud-patch-automation/gcp"
